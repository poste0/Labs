create table laba_user (
	ID varchar(36) not null,
	email varchar(255),
    	login varchar(255),
    	password varchar(255),
    	firstName varchar(255),
    	lastName varchar(255),
    
    	primary key (ID)
);

